'use client';

import React from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { ArrowRight, Check, ChevronRight, CircleDollarSign, FileSearch, FileText, MessageSquareText, Moon, Scale, Sparkles, Sun, Zap } from 'lucide-react';
import { useAuth } from '@/context/AuthContext';
import { Button } from '@/components/ui';
import { ALXOLogo } from '@/components/brand';
import { HeroScene } from '@/components/landing/HeroScene';

const STEPS = [
  { icon: MessageSquareText, number: '01', title: 'Bring the thread', body: 'Upload the conversations where the work actually changed — WhatsApp, Slack, email, or CSV.' },
  { icon: FileSearch, number: '02', title: 'Find the drift', body: 'ALXO compares every request to the original agreement and spots the work that slipped past it.' },
  { icon: Scale, number: '03', title: 'Verify the receipt', body: 'Review the evidence, refine the estimate, and decide exactly what belongs in your ledger.' },
  { icon: FileText, number: '04', title: 'Send with certainty', body: 'Turn verified line items into an impeccably clear change order your client can act on.' },
];
const FEATURES = [
  { title: 'Source-linked decisions', body: 'Every entry can be traced back to the message that created it.', icon: FileText },
  { title: 'Math you can defend', body: 'Hours × rate is calculated in code, never improvised by AI.', icon: CircleDollarSign },
  { title: 'A fixed baseline', body: 'Your original scope stays visible while the project evolves around it.', icon: Scale },
];

function DemoButton({ variant = 'hero' }: { variant?: 'nav' | 'hero' | 'cta' }) {
  const { signInDemo } = useAuth();
  const router = useRouter();
  const goDemo = () => { signInDemo(); router.push('/app/dashboard'); };
  return <Button size={variant === 'nav' ? 'sm' : 'lg'} onClick={goDemo} className={variant === 'hero' ? 'shadow-glow' : undefined}>{variant === 'nav' ? <><Zap className="h-3.5 w-3.5" /> Try the demo</> : <><Zap className="h-4 w-4" /> Open demo workspace <ArrowRight className="h-4 w-4" /></>}</Button>;
}

function PublicNavbar({ isDark, onToggle }: { isDark: boolean; onToggle: () => void }) {
  return <header className="sticky top-0 z-40 border-b border-border/80 bg-background/80 backdrop-blur-xl"><div className="mx-auto flex h-[72px] max-w-7xl items-center px-4 sm:px-6 lg:px-8"><Link href="/" className="flex items-center" aria-label="ALXO home"><ALXOLogo size={29} /></Link><nav className="ml-12 hidden items-center gap-7 text-sm font-medium text-muted-foreground md:flex" aria-label="Primary"><a href="#how-it-works" className="transition-colors hover:text-foreground">How it works</a><a href="#evidence" className="transition-colors hover:text-foreground">The ledger</a><a href="#why-alxo" className="transition-colors hover:text-foreground">Why ALXO</a></nav><div className="ml-auto flex items-center gap-2"><Link href="/sign-in" className="hidden rounded-lg px-3 py-2 text-sm font-medium text-muted-foreground transition-colors hover:text-foreground sm:block">Sign In</Link><button type="button" onClick={onToggle} aria-label={isDark ? 'Switch to light mode' : 'Switch to dark mode'} className="rounded-lg p-2 text-muted-foreground transition-colors hover:bg-muted hover:text-foreground">{isDark ? <Sun className="h-4 w-4" /> : <Moon className="h-4 w-4" />}</button><DemoButton variant="nav" /></div></div></header>;
}

function EvidencePreview() {
  return <div className="relative mx-auto max-w-5xl"><div className="absolute -inset-x-12 -top-12 h-48 rounded-full bg-brand-ink/10 blur-3xl" /><div className="relative overflow-hidden rounded-2xl border border-border bg-card shadow-card-hover"><div className="flex items-center justify-between border-b border-border bg-muted/50 px-5 py-3 sm:px-6"><div className="flex items-center gap-3"><span className="h-2.5 w-2.5 rounded-full bg-success" /><span className="font-mono text-[11px] font-medium uppercase tracking-[0.16em] text-muted-foreground">Verified scope ledger</span></div><span className="rounded-full border border-success/20 bg-success/10 px-2.5 py-1 text-xs font-semibold text-success">₹ 36,800 recovered</span></div><div className="grid lg:grid-cols-[1.15fr_0.85fr]"><div className="p-5 sm:p-7"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-wider text-info">Evidence #004</p><h3 className="mt-1 text-lg font-bold text-foreground">Mobile checkout redesign</h3></div><span className="rounded-md bg-warning/15 px-2 py-1 text-xs font-semibold text-warning">New request</span></div><blockquote className="mt-6 border-l-2 border-brand-accent pl-4 font-mono text-sm leading-6 text-foreground sm:text-base">“Can we make the checkout work beautifully on mobile too? The current flow feels cramped.”</blockquote><div className="mt-5 grid grid-cols-3 gap-3 border-t border-border pt-5"><div><p className="text-[11px] uppercase tracking-wider text-muted-foreground">Confidence</p><p className="mt-1 text-sm font-semibold text-foreground">94%</p></div><div><p className="text-[11px] uppercase tracking-wider text-muted-foreground">Effort</p><p className="mt-1 text-sm font-semibold text-foreground">8 hours</p></div><div><p className="text-[11px] uppercase tracking-wider text-muted-foreground">Value</p><p className="mt-1 text-sm font-semibold text-success">₹ 12,800</p></div></div></div><aside className="border-t border-border bg-muted/30 p-5 lg:border-l lg:border-t-0 sm:p-7"><p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Why it’s out of scope</p><p className="mt-3 text-sm leading-6 text-foreground">The original agreement covers desktop checkout only. Responsive design was not included in the deliverables.</p><div className="mt-6 rounded-xl border border-border bg-card p-4"><p className="font-mono text-[11px] uppercase tracking-wider text-muted-foreground">Original agreement</p><p className="mt-2 text-sm text-foreground">“Design and build a 5-page marketing site and desktop checkout.”</p></div><div className="mt-5 flex items-center gap-2 text-sm font-semibold text-success"><Check className="h-4 w-4" /> Ready for change order</div></aside></div></div></div>;
}

export default function LandingPage() {
  const { isDarkMode, toggleTheme } = useAuth();
  return <div className="min-h-screen overflow-hidden bg-background text-foreground"><PublicNavbar isDark={isDarkMode} onToggle={toggleTheme} /><main><section className="relative isolate min-h-[670px] px-4 pb-16 pt-16 sm:px-6 lg:min-h-[740px] lg:px-8 lg:pt-24"><HeroScene /><div className="relative z-10 mx-auto max-w-4xl text-center"><p className="inline-flex items-center gap-2 rounded-full border border-info/25 bg-background/65 px-3 py-1.5 text-xs font-semibold text-info shadow-sm backdrop-blur"><Sparkles className="h-3.5 w-3.5" /> Scope intelligence for independent teams</p><h1 className="mx-auto mt-7 max-w-4xl text-5xl font-extrabold leading-[0.98] tracking-[-0.055em] text-foreground sm:text-6xl lg:text-7xl">Stop doing extra work for free.<br /><span className="bg-brand-gradient bg-clip-text text-transparent">Your agreement should too.</span></h1><p className="mx-auto mt-7 max-w-2xl text-base leading-7 text-muted-foreground sm:text-lg">ALXO turns scattered client conversations into a calm, evidence-backed case for the work that wasn’t part of the plan.</p><div className="mt-8 flex flex-col items-center justify-center gap-3 sm:flex-row"><DemoButton /><Link href="/request-access"><Button variant="outline" size="lg">Request access <ArrowRight className="h-4 w-4" /></Button></Link></div><p className="mt-4 text-xs text-muted-foreground">No credit card · Demo data is ready to explore</p></div><div className="relative z-10 mx-auto mt-16 grid max-w-3xl grid-cols-3 border-y border-border/80 bg-background/35 py-5 text-center backdrop-blur-sm">{[['Every request', 'linked to source'], ['Every number', 'calculated in code'], ['Every ask', 'ready to send']].map(([top, bottom]) => <div key={top} className="px-3 text-xs sm:text-sm"><span className="block font-semibold text-foreground">{top}</span><span className="text-muted-foreground">{bottom}</span></div>)}</div></section><section id="how-it-works" className="mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8"><div className="grid gap-12 lg:grid-cols-[0.85fr_1.15fr] lg:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-info">The flow</p><h2 className="mt-4 text-3xl font-bold tracking-tight text-foreground sm:text-4xl">From client chat to billing evidence in four steps</h2></div><p className="max-w-xl text-base leading-7 text-muted-foreground">Built for the awkward gap between what was agreed and what quietly became expected.</p></div><div className="mt-12 grid gap-px overflow-hidden rounded-2xl border border-border bg-border sm:grid-cols-2 lg:grid-cols-4">{STEPS.map((step) => { const Icon = step.icon; return <article key={step.number} className="group bg-card p-6 transition-colors hover:bg-muted/40"><div className="flex items-center justify-between"><span className="font-mono text-xs text-muted-foreground">{step.number}</span><Icon className="h-5 w-5 text-brand-accent" /></div><h3 className="mt-12 text-lg font-semibold text-foreground">{step.title}</h3><p className="mt-3 text-sm leading-6 text-muted-foreground">{step.body}</p></article>; })}</div></section><section id="evidence" className="border-y border-border bg-muted/35 px-4 py-24 sm:px-6 lg:px-8"><div className="mx-auto mb-12 max-w-2xl text-center"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-info">Not another dashboard</p><h2 className="mt-4 text-3xl font-bold tracking-tight text-foreground sm:text-4xl">A ledger built on evidence, not vibes</h2><p className="mt-4 text-base leading-7 text-muted-foreground">The story is always visible: the new request, the original promise, and the real impact.</p></div><EvidencePreview /></section><section id="why-alxo" className="mx-auto grid max-w-7xl gap-12 px-4 py-24 sm:px-6 lg:grid-cols-[0.9fr_1.1fr] lg:items-center lg:px-8"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-info">Clarity compounds</p><h2 className="mt-4 text-3xl font-bold tracking-tight text-foreground sm:text-4xl">Why it matters: protect the relationship, not just the margin.</h2><p className="mt-5 max-w-lg text-base leading-7 text-muted-foreground">Scope creep is rarely a confrontation waiting to happen. It’s a record waiting to be made clear — while the details still matter.</p><Link href="/sign-in" className="mt-7 inline-flex items-center gap-2 text-sm font-semibold text-primary transition-colors hover:text-brand-accent">See how ALXO works <ChevronRight className="h-4 w-4" /></Link></div><div className="grid gap-4 sm:grid-cols-3">{FEATURES.map((feature) => { const Icon = feature.icon; return <div key={feature.title} className="rounded-xl border border-border bg-card p-5 shadow-card"><span className="flex h-9 w-9 items-center justify-center rounded-lg bg-primary/10 text-primary"><Icon className="h-4 w-4" /></span><h3 className="mt-7 text-base font-semibold text-foreground">{feature.title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{feature.body}</p></div>; })}</div></section><section className="px-4 pb-24 sm:px-6 lg:px-8"><div className="mx-auto max-w-5xl overflow-hidden rounded-2xl bg-brand-gradient px-6 py-14 text-center shadow-card sm:px-12"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-primary-foreground/70">A better way to draw the line</p><h2 className="mx-auto mt-4 max-w-2xl text-3xl font-bold tracking-tight text-primary-foreground sm:text-4xl">Stop letting valuable work disappear into the thread.</h2><p className="mx-auto mt-4 max-w-xl text-sm leading-6 text-primary-foreground/80 sm:text-base">Open the demo and see what a defensible scope conversation feels like.</p><div className="mt-8"><DemoButton variant="cta" /></div></div></section></main><footer className="border-t border-border px-4 py-8 sm:px-6 lg:px-8"><div className="mx-auto flex max-w-7xl flex-col items-center justify-between gap-4 text-xs text-muted-foreground sm:flex-row"><ALXOLogo size={22} /><p>© {new Date().getFullYear()} ALXO · Catch the work hiding between the lines.</p><Link href="/request-access" className="hover:text-foreground">Request access</Link></div></footer></div>;
}
