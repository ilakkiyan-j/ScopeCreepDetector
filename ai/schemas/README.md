# AI Schemas Directory

This directory stores JSON Schema files used to validate Amazon Bedrock outputs.

Planned files:
- `classification-response.schema.json`: JSON schema enforcing classification outputs (`message_id`, `classification`, `confidence`, `reason`, `estimated_hours`).
- `change-order-response.schema.json`: JSON schema for structured email draft generation.
